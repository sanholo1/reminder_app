# Production environment configuration
NODE_ENV=production

# Database Configuration
DB_HOST=database
DB_PORT=3306
DB_NAME=reminder_app
DB_USER=reminder_user
DB_PASSWORD=your_secure_database_password_here
DB_ROOT_PASSWORD=your_secure_root_password_here

# JWT Configuration (MUST be 32+ characters)
JWT_SECRET=your_super_secure_jwt_secret_key_that_is_very_long_and_random_12345

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Port Configuration
FRONTEND_PORT=3000
BACKEND_PORT=3001
DB_PORT=3306
REDIS_PORT=6379

# Optional: Enable Redis session storage
# Uncomment the line below and set --profile with-redis when running docker-compose
# COMPOSE_PROFILES=with-redis